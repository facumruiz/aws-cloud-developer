{
  "name": "02-serverless-crud-api",
  "version": "1.0.0",
  "private": true,
  "description": "CRUD API serverless (API Gateway + Lambda + DynamoDB) sobre LocalStack",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
